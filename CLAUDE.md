# Claude

Cross-repo context, conventions, history, and backlog live in
**github.com/universal-field-robots/claude-context** — read its `CLAUDE.md` first.

Record fixes (`postmortems/`), choices (`decisions/`), and future work
(`backlog/`) there; keep entries brief. This repo's API is consumed by many
frontends — don't break it.
